# Company domain-pack template

Copy this directory, choose a unique `profile_id`, register source contracts, and define declarative
column mappings. Mapping occurs before generic feature construction. Secrets and executable Python do
not belong in profile YAML. Complex transformations require reviewed adapter code.

