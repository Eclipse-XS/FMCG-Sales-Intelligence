# Portfolio capture guide V1.3

Do not commit screenshots unless captured from the current validated release. Useful real screens are:

1. Dashboard home with global filters and KPI overview.
2. Forecast view with seven-day horizon and model metadata.
3. Stockout view with score, frozen threshold and decision.
4. Segmentation view with exploratory status visible.
5. Anomaly candidate review.
6. Basket association rules.
7. Promotion before/during/after summaries labelled descriptive.
8. MLflow canonical run and model-alias view.
9. Grafana engineering dashboard.

Before capture, verify `/ready`, use synthetic demo data, hide local paths and credentials, and retain limitation labels. No existing screenshot set was treated as current release evidence.
